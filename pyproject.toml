[project]
name = "sihproject"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "motor",
    "python-dotenv",
    "numpy",
    "requests"
    
]

[tool.vercel]
entrypoint = "backend.app.main:app"
